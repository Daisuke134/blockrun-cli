# Handover — blockrun-cli (2026-07-08 13:16 JST)

Repo: `github.com/Daisuke134/blockrun-cli` (public, Daisuke134). Local: `~/blockrun-cli`. Branch: main.

## 今回やったこと
- BlockRun MCP server (`BlockRunAI/blockrun-mcp` v0.28.0, TypeScript) の**全18ツールを 1:1 で CLI サブコマンド化**した `blockrun-cli` を新規構築し、VCSDD strict パイプラインを**全フェーズ完走 (phase=complete)**。
- 調査: blockrun-mcp 全ソース + CONTRIBUTING + @blockrun/llm SDK を subagent 2体で解析。org 31 repo / npm 全パッケージを確認し「汎用 blockrun CLI は非存在＝車輪の再発明でない」を確定。CLI-Anything (HKUDS) は方法論のみ採用、実装言語は TS（org 全体が TS 主力のため）。
- VCSDD: init(strict) → spec v4 (REQ131/PROP74) → spec-review (Opus adversary 3 iteration + codex 3 round、全 PASS 収束) → contract (sprint-1 承認 + Opus contract-review PASS) → tdd RED (159 fail) → impl Green (自己検証 408/408) → **impl-review Phase 3 (money-path blocking 7件を codex+Opus 独立検出→修正→ iteration-2 で PASS)** → harden Phase 5 (purity CLEAN / security PASS / 408-test proof) → converge Phase 6 (4次元収束 PASS)。
- **実課金 E2E 18/18 全通過**（自分で実行）。sandbox HOME に $0.59 USDC 着金 → 実 BlockRun API に対し全コマンド実行、実支出 ≈$0.325。証拠は `VERIFICATION.md`（tx hash / MD5 / response id / 実コスト / chain / wallet / HOME）。
- adversary が見逃した実バグ2件を**自分の実機 smoke-run が発見**: (1) fresh HOME 初回 `wallet` が default chain を solana に誤フリップ (FIND-007、REQ-016a で on-demand solana wallet 化して修正) (2) video quote-gate の $0.00 表示バグ（修正済 $0.0010）。

## 決定事項
- 実装言語 = **TypeScript**（blockrun-mcp と同規約: ESM, zod, node:test+tsx, tsup, kebab-case, 1コマンド1ファイル, formatError, budget reserve）。CLI-Anything の Python 生成物は不採用。
- `@blockrun/llm` SDK を wrap、x402署名/wallet は**絶対に自前再実装しない**（REQ-221/222、構造的型キャストで SDK メソッド呼び出し）。
- adversary spawn は全て `model: opus`（Opus 4.8）。codex-review を別モデル独立視点として重ねる（Opus が見逃した money-path bug を codex が検出＝多視点の実証）。
- budget 3層: ephemeral `--budget-limit` / 永続 `~/.blockrun/cli-budget.json` / video `--max-quote-usd`。永続 ledger は MCP の in-memory を CLI one-shot 用に意図的 adaptation（REQ-019系）。
- E2E 予算上限 $10、実支出 $0.325。高額/不可逆パス（phone numbers/buy $5、realface enroll=liveness要）は untested として ledger 明記（隠さない）。

## 捨てた選択肢と理由
- **CLI-Anything で Python CLI 生成** → 却下。出力が Python/Click で blockrun-mcp (TS) と規約がミスマッチ、3人チームのレビューに通らない。方法論（--json, 4層テスト, quote-gate 思想）のみ採用。
- **REQ-016 precedence 定義の変更** → 却下。clone 1:1 を保持し、代わりに ensureBothWallets の呼び出しタイミング＝ solana wallet を on-demand 生成（REQ-016a）で解決。
- **npm publish** → スコープ外（repo 公開まで）。@blockrun name space は入社後にチームと。
- Polymarket $19.26 を E2E 資金に → 却下（未証明の bridge 経路）。Aave withdraw で $0.59 確保。

## ハマりどころ
- VCSDD state lib の gate が厳格: bead schema (`BEAD-\d{3,}`, enum type, linkedBeads)、green log マーカー (`target-feature-tests: PASS`)、sprint contract frontmatter (CRIT-XXX)、grading verdict (PASS次元は findings空+evidence必須)、harden report 見出し (`## Proof Obligations` 等)、verdict は `reviews/sprint-1/output/` に配置。全て手で満たした。
- codex CLI が npx cache 破損 (`@solana/web3.js` 欠落 → codex-darwin-arm64 ENOENT)。`npm i -g @openai/codex` で再インストール。stdin 開放で hang → `</dev/null` 必須。
- 複数 subagent が同一 branch で並行編集（worktree 未分離）→ commit 帰属が混ざったが内容ロスなし。builder が fetch-before-push で回避。
- `gog gmail send` は `GOG_KEYRING_PASSWORD`（~/.openclaw/.env）を env に読み込む必要あり。

## 学び
- 実機 smoke-run（fresh HOME で実バイナリを叩く）が adversary の静的レビューを補完し、2件の実バグを掘った。「コンパイル通過≠完了、自分で E2E」の実証。
- codex(別モデル) と Opus adversary の**独立並行レビュー**が同じ money-path bug を突いた＝多視点の裏取りが機能。

## 次にやること（優先順）
1. **README.md 作成**（最優先、まだ無い）— blockrun-mcp README と同等構成: title/badges, Install (`npx blockrun` / clone+build), Fund wallet, **Tools テーブル18行**, Usage 例 (--json/--help), Env vars, Multi-agent budget delegation, Troubleshooting, How it works, Contributing リンク。
2. **CHANGELOG.md** — blockrun-mcp 形式 (`## X.Y.Z` + `**\`tool\` — 見出し。** 説明` bullet)。初版 1.0.0。
3. **CONTRIBUTING.md** — blockrun-mcp の Tool-vs-Skill / description上限 / test規約 / x402パターンを CLI 版に。
4. **LICENSE** (MIT or blockrun-mcp と同じ)、package.json の description/keywords/repository/homepage 整備。
5. **全出力の最終検証**: README の全コマンド例が実機で動く / 全18 `--help` が出る / VERIFICATION.md の生成物 URL が実在。
6. 最終 push、必要なら 1.0.0 tag。

## 関連ファイル
- `~/blockrun-cli/src/{core,args,cli,shell,commands}/*.ts` + `index.ts`（実装、66ファイル）
- `~/blockrun-cli/test/{unit,integration,cli}/*.test.ts`（408テスト）
- `~/blockrun-cli/VERIFICATION.md`（E2E 18/18 ledger）
- `~/blockrun-cli/.vcsdd/features/blockrun-cli/`（spec/contract/reviews/verification/state）
- `~/blockrun-cli/.vcsdd/features/blockrun-cli/specs/decisions.md`（実装判断の全記録、§8-14）
- 参照 clone: `/private/tmp/claude-501/.../scratchpad/blockrun-mcp`（README/CONTRIBUTING の手本）
- E2E: sandbox HOME `~/blockrun-cli-e2e-home`、出力 `~/blockrun-cli-e2e-out/`

## 新セッション開始プロンプト
`~/blockrun-cli` の VCSDD は complete（18/18 E2E 済、VERIFICATION.md あり）。残りは docs 整備のみ。blockrun-mcp (`/private/tmp/claude-501/-Users-anicca-anicca-project/ec3606df-8de7-491a-8a92-7ee667020d6a/scratchpad/blockrun-mcp`) の README/CONTRIBUTING を手本に、blockrun-cli の README.md（Tools 18行テーブル含む）・CHANGELOG.md（1.0.0）・CONTRIBUTING.md・LICENSE・package.json メタを作成し、README の全コマンド例と全18 `--help` を実機（`node dist/index.js`）で検証してから commit+push。全出力が実際に動くことを確認するまで完了としない。
