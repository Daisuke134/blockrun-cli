# blockrun-cli — Tier-3 End-to-End Verification Ledger

Every one of the 18 commands exercised against the **real BlockRun API** (blockrun.ai), with
real x402 USDC micropayments on Base mainnet where the tool is paid. This is the evidence ledger
required by the project goal and `verification-architecture.md` §4.3–4.5. No mocks, no dry runs.

## Environment (attributable & reproducible)

| Field | Value |
|---|---|
| Binary | `dist/index.js` (built via `npm run build`, tsup ESM) |
| Sandbox HOME | `/Users/anicca/blockrun-cli-e2e-home` (isolated per REQ-017 — SDK reads `os.homedir()`) |
| Active chain | `base` |
| Base wallet | `0xa5CeF4943c3F8f34e5138b5BcdE6B88746a5c804` (auto-generated by the SDK in the sandbox HOME) |
| Solana wallet | `HxeDzzgrMjZFnqqrEj6iZyiqx3XbeQ2Ke4ZcgdSshLZm` |
| Funding tx | `0xccbaf5adeb67e2e144be9dd091b9533a951eb7c2ea5189dff0a02e0d33f4bbe3` — $0.59 USDC from `0x810f6d61f7606deee2657d3083e150a222bc29c5` |
| Start balance | $0.590 USDC |
| End balance | $0.264748 USDC |
| Real spend | ≈ $0.325 (persisted ledger `cli-budget.json`: `spent` $0.316001 across 19 paid `calls`; on-chain settlement + the two portrait-retry face images account for the small delta) |
| Budget cap | $10 USDC (goal) — used ≈ 3.25% |
| Date | 2026-07-07 (UTC) |

## The 18-command ledger — 18/18 PASS

| # | Command | Real path exercised | Primary evidence (response id / tx / artifact) | Cost | Result |
|---|---|---|---|---|---|
| 1 | `wallet` | `status`→`chain`→`delegate`(agent e2e-test $0.05)→`report` — **4 separate processes**, verifying the delegation persisted to `~/.blockrun/cli-budget.json` (REQ-019/PROP-103) | `report` returned the agent entry written by a *prior separate invocation*: `{"e2e-test":{"limit":0.05,...,"remaining":0.05}}` | $0 | PASS — cross-process persistence proven |
| 2 | `models` | default catalog list | 74 models returned | $0 | PASS |
| 3 | `dex` | `--query SOL` | real DexScreener pairs (aerodrome/base…) | $0 | PASS |
| 4 | `price` | `--action price --category crypto --symbol BTC-USD` | Pyth feed `0xe62df6c8…`, price 63986.997, publishTime 1783444867 | $0 | PASS |
| 5 | `chat` | `--mode free --message "…2+2?"` | `{"model_used":"nvidia/llama-4-maverick","response":"4"}` | $0 | PASS |
| 6 | `defi` | `--path prices/coingecko:bitcoin` | `{"coins":{"coingecko:bitcoin":{"price":63889.65,…}}}` | $0.001 | PASS |
| 7 | `surf` | `--path market/price --params '{"symbol":"BTC"}'` | asksurf `MarketPriceResponse`, project_id 25c6612a…, BTC series | $0.001 | PASS |
| 8 | `markets` | `--path polymarket/events` | live Polymarket events (Tallahassee mayoral…) | $0.001 | PASS |
| 9 | `rpc` | `--network base --method eth_blockNumber` | `{"id":1,"jsonrpc":"2.0","result":"0x2e16c6b"}` (real Base head) | $0.002 | PASS |
| 10 | `exa` | `--path contents --body '{"urls":["https://blockrun.ai"]}'` | requestId `518dbb4e05447dd00b85a8beac638f45`, real page text | $0.002 | PASS |
| 11 | `search` | `--query "blockrun ai gateway" --max-results 1` | Grok live-search summary of BlockRun | $0.025 | PASS |
| 12 | `modal` | `sandbox/create`→`status`→`terminate` (full lifecycle) | sandbox `sb-C1EZrJXCKY2X40cDw9PGeu`: running → terminated | ~$0.012 | PASS |
| 13 | `image` | `--model zai/cogview-4 --prompt "a red cube…"` | `…/83a93a29-….png`, downloaded → real 1024×1024 image, **MD5 `dc8a4cad2060539d2cb33392082031c3`** | $0.015 | PASS |
| 14 | `speech` | `--action speak --input "hi"` | mp3, tx `0xd8184055ef5999b21dd5ea33209396d5726d6e50bc45fea273267ed56b285815` | $0.001 | PASS |
| 15 | `music` | `--prompt "chill lo-fi beats"` (default model) | `…/c8467a3b-….mp3`, 84s, tx `0xaf974e98fe8183a311c7a37ad68a08cf701ecee727498c4351dd301115e5eeac` | $0.1575 | PASS |
| 16 | `video` | quote-gated: `--model xai/grok-imagine-video --duration-seconds 1 --resolution 360p --max-quote-usd 0.10` | `…/b839a3fd-….mp4`, 1s, quote $0.0525 ≤ cap → signed, tx `0xa4625b8102c223d7733bf3d1a92a95769da7eb87d7992df5b6ef48d28f256c42` | $0.0525 | PASS |
| 16b | `video` (gate) | abort path: `--max-quote-usd 0.001` (quote $0.0525 > cap) | exit 1, aborted **before** `createPaymentPayload`, balance unchanged $0.310498→$0.310498 | $0 | PASS — no-charge gate proven |
| 17 | `realface` | `init`/`status`/`list` (free) → `portrait` with an AI-generated face | asset `ta_c97289c307c2430f8c3e5e1c75694c05`, group `legacy_rf_17921`, price $0.01 | $0.01 | PASS (first attempt hit an upstream 502; a retry — goal's "3 approaches" rule — settled) |
| 18 | `phone` | `--path phone/numbers/list --body '{}'` | `{"numbers":[],"count":0}` (real, empty inventory) | $0.001 | PASS |

## Notes (honest)

- **realface #17** — the first `portrait` call returned upstream `API error 502: Request failed`
  (BlockRun-side, not a CLI defect; the CLI classified it correctly and exited non-zero with no
  charge, since realface is payment-on-completion). A retry ~seconds later succeeded. Recorded, not hidden.
- **video gate display (minor)** — the abort message formats the cap with 2 decimals, so
  `--max-quote-usd 0.001` prints as `$0.00` in the error string. The *gate itself is correct*
  (aborted before signing, zero charge); only the displayed number rounds. A follow-up fix is
  in flight to show sub-cent caps at full precision. The gate's money-safety behavior is unaffected.
- **Untested-by-design (per §4.4, documented not hidden):** `realface enroll` (requires a live
  human liveness scan — `init` returned the BytePlus `h5_link` proving this real gate exists),
  `phone numbers/buy` ($5 irreversible), `phone voice/call` ($0.54); Solana-side payment paths
  (sandbox funded on Base only). Each command's *cheapest real path* was exercised instead.
- **Persisted ledger** — after the run, `~/.blockrun/cli-budget.json` reads
  `global.spent = $0.316001, calls = 19`, and still holds the `e2e-test` agent delegation —
  proving the persistence layer tracked every real settlement across 19 separate one-shot processes.

**Result: 18/18 commands verified end-to-end against the live BlockRun API. Total real spend
≈ $0.325 USDC, well inside the $10 cap and the $0.59 funding.**
